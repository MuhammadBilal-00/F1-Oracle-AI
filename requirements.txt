# Core — PINNED so deployed servers reproduce the exact environment the
# model .joblib artifacts were trained/pickled with (mismatched versions
# make predict_proba fail at runtime -> all-zero predictions).
pandas==2.2.3
numpy==2.2.5
scipy==1.15.3
statsmodels==0.14.6

# Database
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
aiofiles>=23.0.0
python-multipart>=0.0.6

# Machine Learning — PINNED (must match training versions)
scikit-learn==1.7.0
xgboost==3.2.0
lightgbm==4.6.0
catboost==1.2.10
imbalanced-learn==0.14.1

# Explainability
shap==0.51.0

# MLOps
mlflow>=2.9.0
joblib==1.5.1
optuna>=3.4.0

# Visualization
plotly>=5.18.0

# Utilities
pyarrow==20.0.0
